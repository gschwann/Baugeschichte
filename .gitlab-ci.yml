image: registry.gitlab.com/gschwann/qtandroiddocker:latest

stages:
- build

build:
  stage: build
  script:
    - echo "$KEYSTORE_FILE" > GrazWikiKeyStore.jks
    - ./scripts/buildAndroid.sh
    - rm GrazWikiKeyStore.jks
  artifacts:
    paths:
      - build/apk
    expire_in: 15 week
